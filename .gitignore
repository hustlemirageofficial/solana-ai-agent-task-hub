node_modules

.env
.env.local
.DS_Store
.cache
.output
.nitro
.tanstack

/dist/
/dist-ssr/

# Vercel Build Output API bundle (produced by build-vercel.sh at go-live).
/.vercel/

# Runtime server log written on start.
/.run/

# Generated by the TanStack Start router plugin on dev/build.
src/routeTree.gen.ts

# AgentPay escrow keypair (auto-generated at first boot when ESCROW_PRIVATE_KEY is unset).
/data/

# Environment variants — only .env.example is committed.
.env.*
!.env.example
# Runtime / temp files.
*.log
nohup.out
